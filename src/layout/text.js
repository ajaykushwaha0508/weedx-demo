import React from 'react';

const text = ({data}) => {
    return (
        <div>
              <p className="m-0">{`Find Nearby Weed Delivery in ${data}  for Recreational & Medical Uses. Browse Top Cannabis Products and Place Orders from Trusted weed delivery near you.`}</p>
        </div>
    );
};

export default text;